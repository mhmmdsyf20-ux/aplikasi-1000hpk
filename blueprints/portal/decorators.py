from services.ibu_service import portal_login_required

# Backward-compatible module export: keep `portal_login_required` available
# as before so code importing from blueprints.portal.decorators keeps working.
